import Controller from '../utils/Controller';

export default class FeedbackTypeController extends Controller {
  // In all of project secttion you can read user info from request
  // request['user'] include all user information you required.
  //You can use request['user'] for access to user information
  //You can log request['user']
}
